# Migration `20201031184112-command-visited-nodes`

This migration has been generated by Angad Singh at 11/1/2020, 12:11:12 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `UserMeta` ADD COLUMN `currentNode` int  NOT NULL DEFAULT 0

CREATE TABLE `Command` (
`id` int  NOT NULL  AUTO_INCREMENT,
`created_at` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`updated_at` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`command` varchar(191)  NOT NULL ,
`ip` varchar(191)  NOT NULL ,
`userId` int  NOT NULL ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `VisitedNodes` (
`id` int  NOT NULL  AUTO_INCREMENT,
`created_at` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`updated_at` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`node` int  NOT NULL DEFAULT 0,
`userId` int  NOT NULL ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `Command` ADD FOREIGN KEY (`userId`) REFERENCES `UserMeta`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `VisitedNodes` ADD FOREIGN KEY (`userId`) REFERENCES `UserMeta`(`id`) ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201029090839-init..20201031184112-command-visited-nodes
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -75,7 +75,27 @@
   username     String?   @unique
   admin        Boolean   @default(false)
   exunite      Boolean   @default(false)
   institution  String?
+  currentNode  Int       @default(0)
   points       Int       @default(0)
   lastActive   DateTime  @default(now())
 }
+
+model Command {
+  id           Int       @default(autoincrement()) @id
+  createdAt    DateTime  @default(now()) @map(name: "created_at")
+  updatedAt    DateTime  @default(now()) @map(name: "updated_at")
+  command      String
+  ip           String
+  userId       Int
+  user         UserMeta  @relation(fields: [userId], references: [id])
+}
+
+model VisitedNodes {
+  id           Int       @default(autoincrement()) @id
+  createdAt    DateTime  @default(now()) @map(name: "created_at")
+  updatedAt    DateTime  @default(now()) @map(name: "updated_at")
+  node         Int       @default(0)
+  userId       Int
+  user         UserMeta  @relation(fields: [userId], references: [id])
+}
```


